<%- include('parts/header') %>
<%- include('parts/navbar') %>

<br>
<div class="container">


   



    <% if(orders.length === 0 ) {%>
        <p class="alert alert-danger">
            There Is No Cart In Orders
        </p>
        <%} else {%>

        <table border="1" >
            <thead>
                <td>No</td>
                <td>Product Name</td>
                <td>Amount</td>
                <td>Cost</td>
                <td>address</td>
                <td>Status</td>
                <td>Time</td>
                <td></td>
            </thead>
            <tbody>
                <% for(let i=0;i<orders.length ;i++) { %>
                    <tr>
                        <td><%= i+1%></td>
                        <td><%=orders[i].name%></td>
                        <td><%=orders[i].amount%></td>
                        <td><%=orders[i].cost %></td>
                        <td><%=orders[i].address %></td>
                        <td><%=orders[i].status %></td>
                        <td><%=orders[i].time %></td>
                        <form method="POST"> 
                            <td>
                                <input type="hidden" name="orderId" value="<%=orders[i]._id%>">
                                <input type="submit" class="btn btn-primary" value="Cancel" formaction="/order/delete">

                            </td>
                        </form>
                    </tr>
                    <%}%>
            </tbody>
            
        </table>


            <%}%>


            <br>
            <br>
            <div>
                    <% if(orders.length != 0 ) {%>

                <form method="POST">
                    <input type="submit" value="Cancel All " formaction="/order/delete-all">
                    <!-- <input type="submit" value="Order All " formaction="/cart/order-all"> -->
                </form>
                <%} %>

            </div>


</div>




<%- include('parts/footer')%></div>